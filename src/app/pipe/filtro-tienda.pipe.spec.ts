import { FiltroTiendaPipe } from './filtro-tienda.pipe';

describe('FiltroTiendaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTiendaPipe();
    expect(pipe).toBeTruthy();
  });
});
